# T를 받으세요
T = int(input())

# T회 반복
for i in range(T):
    # stack list를 선언하고 입력 한 줄을 받으세요
    stack = []
    ps = input()
    
    # 입력에 대한 연산
    for item in ps:
        # stack이 비지 않았을때
        if stack:
            # item: "("
            if item =='(':
                continue # 여기를 수정하세요
            # item: ")"
            else:
                         #
                         #
                         #
                continue # 여기를 수정하세요
        # stack이 비었을때
        else:
            continue # 여기를 수정하세요

    # 한 줄에 대한 결과 출력



